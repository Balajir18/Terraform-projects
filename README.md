# Terraform-projects
Terraform project deploying an EC2 instance and an S3 bucket using modular architecture. A custom VPC module creates subnets, route tables, and networking, and the EC2 instance launches inside this VPC. Infrastructure is fully automated using terraform init, plan, apply, and destroy.
